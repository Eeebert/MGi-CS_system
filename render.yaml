services:
  - type: web
    name: mgi-cs-system
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SUPABASE_DB_URL
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: DB_SCHEMA_MODE
        value: validate
